{
  "name": "speed-layer",
  "version": "2.1.0",
  "description": "Client-side script management system for dealership websites",
  "main": "server/index.js",
  "bin": {
    "speed-layer": "./cli/index.js"
  },
  "scripts": {
    "start": "node server/index.js",
    "validate": "node cli/index.js validate",
    "lint": "node -e \"const fs=require('fs');fs.readdirSync('manifest').forEach(f=>{try{JSON.parse(fs.readFileSync('manifest/'+f,'utf8'));console.log('OK',f)}catch(e){console.error('FAIL',f,e.message);process.exit(1)}})\""
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "commander": "^12.1.0",
    "express": "^4.21.2",
    "js-yaml": "^4.1.0",
    "swagger-ui-express": "^5.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
